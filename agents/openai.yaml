interface:
  display_name: "녹음-메모 자동화"
  short_description: "누락된 강의 녹음 이름 변경 및 메모 첨부"
  default_prompt: "Use $lecture-recording-organizer to reconcile missing lecture recording names and Apple Notes attachments without transcription checks."

policy:
  allow_implicit_invocation: true
