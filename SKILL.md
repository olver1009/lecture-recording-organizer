---
name: lecture-recording-organizer
description: Rename Apple Voice Memos lecture recordings and attach each recording once to the matching Apple Note by date and course. Use for scheduled, manual, or catch-up runs of this lecture workflow; transcription handling is out of scope.
---

# 녹음-메모 자동화

목표는 학기 중 강의 녹음의 제목을 맞추고 정확한 과목·날짜 메모에 파일 하나를 첨부하는 것이다. 전사 상태는 확인하거나 유도하지 않는다.

이 작업 중 사용자에게 질문하거나 응답을 기다리지 않는다. 판단이 모호하거나 화면 잠금·권한·앱 상태 때문에 처리할 수 없는 항목은 건너뛰고 마지막에만 보고한다. Voice Memos 제목 변경과 Notes 첨부 추가는 승인된 작업이다. 파일·녹음·메모·첨부를 삭제하지 않는다.

오디오를 재생하지 않는다. Safari·Finder 미리보기·iPhone 미러링·Script Editor를 사용하지 않는다. Voice Memos에서는 「모든 녹음 항목」만 다루며 휴지통·최근 삭제 항목·다른 폴더는 열지 않는다.

## 실행 범위

Asia/Seoul 기준 2026-09-01부터 2026-12-18까지의 녹음을 다룬다. 예약 실행과 수동 실행을 구분하지 않는다. 매번 Voice Memos DB에서 학기 시작일부터 오늘까지를 빠르게 대조해 다음 항목만 처리한다.

- 시간표상 강의인데 제목이 맞지 않는 녹음
- 제목은 맞지만 Notes에 같은 제목·재생시간의 첨부가 없는 녹음

이미 제목과 첨부가 모두 맞는 녹음은 UI에서 다시 열거나 검토하지 않는다. 따라서 예약을 놓친 날짜가 있어도 다음 실행에서 누락분을 보충한다.

GUI 작업을 시작할 때 로그인 세션이 열려 있으면 `caffeinate -diu -t 900`으로 이번 실행 중 유휴 잠금을 예방한다. 이미 화면이 잠겨 있거나 앱 제어 권한을 새로 요구하면 기다리거나 질문하지 말고 처리 가능한 읽기 작업만 끝낸 뒤 `화면 잠금/권한으로 보류`라고 보고한다. 다음 실행에서 다시 대조한다.

## 고정 시간표

- 월 09:00 미주지역지리, 13:00 지도학및실습, 16:30 도시지리학특강
- 화 15:00 응용 지형학, 16:30 기후변화와 미래환경
- 수 10:30 미주지역지리, 13:00 지도학및실습, 15:00 도시지리학특강
- 목 15:00 응용 지형학, 16:30 기후변화와 미래환경
- 금요일 수업 없음

Voice Memos DB의 `ZDATE` 시작 시각만 사용한다. 파일 생성·수정·종료 시각과 재생시간은 과목 판정에 쓰지 않는다. 같은 날짜의 가장 가까운 수업 시작과 ±15분 이내에서 유일하게 맞을 때만 처리한다.

표준 이름은 `M/D 과목명` 또는 `M/D 과목명 1/n`이다. 현재 제목이 무엇이든 시작 시각으로 계산한 이름과 다르면 수정 후보로 삼는다. 같은 실행에서 같은 날짜·과목의 새 녹음이 여러 개면 시작 시각 순으로 `1/n`, `2/n`을 붙인다. 이전에 사용자가 붙인 분할 번호는 다시 정렬하지 않는다.

## 빠른 대조와 이름 변경

다음 DB를 읽기 전용으로 한 번 조회한다.

`~/Library/Group Containers/group.com.apple.VoiceMemos.shared/Recordings/CloudRecordings.db`

```sql
SELECT Z_PK, ZUNIQUEID, ZCUSTOMLABELFORSORTING, ZPATH,
       datetime(ZDATE + 978307200, 'unixepoch', 'localtime') AS started_local,
       ZDURATION
FROM ZCLOUDRECORDING
WHERE ZPATH IS NOT NULL AND ZDURATION > 0
ORDER BY ZDATE;
```

코드에서 실행 기간과 오늘 이전 레코드만 남기고 시간표를 대조한다. 실제로 변경하거나 첨부할 후보만 Voice Memos 「모든 녹음 항목」에 존재하는지 확인한다. 목록에 없으면 휴지통 여부를 추측하지 말고 건너뛴다.

이름 변경 직전 `ZDURATION`과 원본 파일 크기를 3초 간격으로 한 번 비교한다. 변하면 녹음 중이므로 보류한다. Computer Use로 제목을 바꾼 뒤 같은 `Z_PK`의 `ZCUSTOMLABELFORSORTING`만 다시 읽는다. 다르면 한 번만 재시도하고, 그래도 다르면 Notes에 첨부하지 않고 `이름 변경 실패`로 보고한다.

## Notes 첨부

과목→Notes 폴더는 다음과 같다: 미주지역지리→미주지역지리, 지도학및실습→지도학, 도시지리학특강→도시지리특강, 응용 지형학→응용지형학, 기후변화와 미래환경→기후변화.

Notes DB를 읽기 전용으로 조회해 정규화한 첨부 제목과 초 단위 반올림 재생시간이 원본과 모두 같은지 먼저 확인한다. 일치하면 완료로 보고 UI를 열지 않는다. 제목은 `/`와 `:` 및 `.m4a` 확장자 차이를 정규화한다.

일치 첨부가 없을 때만 Notes의 「강의 녹음」 아래 정확한 과목 폴더와 제목이 정확히 `M/D`인 메모를 연다. 메모가 0개 또는 2개 이상이면 추측하거나 질문하지 않고 건너뛴다. 메모를 새로 만들지 않는다.

Script Editor와 AppleScript GUI는 사용하지 않는다. 이름 변경이 확인된 Voice Memos 항목을 대상 메모로 직접 드래그하는 방식을 우선 사용한다. 자동 드래그가 실패하면 Notes의 파일 첨부 UI로 정확한 원본 `.m4a` 하나만 선택한다. 첨부 제목이 표준 이름과 다르면 해당 첨부 하나만 이름을 바꾼다.

첨부 후 해당 메모에서 제목과 재생시간만 원본과 대조한다. 복사 중이면 최대 30초까지만 기다린다. 30초 뒤에도 재생시간이 0이거나 원본과 다르면 삭제·재첨부·질문 없이 `첨부 불완전`으로 보고한다. 전사문, 전사 버튼, 요약 패널은 열거나 확인하지 않는다.

## 보고

이름 변경, 새 첨부, 이미 완료, 시간표 불일치, 이름 변경 실패, 메모 누락·중복, 첨부 불완전, 화면 잠금·권한 보류만 짧게 보고한다. 오디오 내용과 전사문은 보고하지 않는다.
