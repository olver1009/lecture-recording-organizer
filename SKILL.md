---
name: lecture-recording-organizer
description: Rename Apple Voice Memos lecture recordings and attach them to matching Apple Notes by date and course, including lightweight transcription checks and bounded retries. Use for this lecture-recording workflow only.
---

# 강의 녹음 정리 및 메모 첨부

Computer Use로 1단계와 2단계를 한 번에 처리한다. 빠르고 반복 실행 가능하게 한다. 오디오를 재생하지 말고, 원본 녹음·메모·첨부 파일을 삭제하지 않는다. Asia/Seoul 시간을 사용한다.

## 범위와 시간표

- Voice Memos에서 현재 보이는 「모든 녹음 항목」 행만 후보로 삼는다. 휴지통/최근 삭제 항목/다른 폴더를 보거나 녹음 디렉터리를 전체 검색하지 않는다.
- Safari와 iPhone 미러링을 사용하지 않는다. Finder 미리보기, Space, 재생 버튼, Voice Memos 행 더블클릭도 하지 않는다. 녹음 중이거나 시간이 변하는 항목은 다음 실행으로 넘긴다.
- 재생하지 않는 메타데이터에서 같은 날짜의 정확한 시작 시각을 읽고, 시간표 시작 시각과 가장 가까운 항목(±10분 이내)으로만 매칭한다. 수업 길이는 매칭에 쓰지 않는다.
- 월 09:00 미주지역지리, 13:00 지도학및실습, 16:30 도시지리학특강
- 화 15:00 응용 지형학, 16:30 기후변화와 미래환경
- 수 10:30 미주지역지리, 13:00 지도학및실습, 15:00 도시지리학특강
- 목 15:00 응용 지형학, 16:30 기후변화와 미래환경
- 금요일 수업 없음

시간표 밖이거나 매칭이 유일하지 않으면 그대로 두고 짧게 보고한다. 같은 날짜·과목 녹음이 여러 개면 시작 시각 순으로 「M/D 과목명 1/n」, 「2/n」처럼 붙이고, 하나면 접미사를 붙이지 않는다.

## 1단계: Voice Memos 이름 변경

1. 「모든 녹음 항목」을 먼저 선택하고 보이는 행의 제목과 파일 식별자를 한 번만 수집한다.
2. 그 행에 한해 읽기 전용 메타데이터로 시작 시각을 확인한다. 가능하면 Voice Memos 공유 저장소의 메타데이터를 사용한다.
3. 제목이 「새로운 녹음…」인 항목 중 유일하게 매칭되는 것만 이름을 바꾼다. 이미 과목명이 있는 녹음도 2단계 후보로 사용한다.
4. 같은 목록에서 이름만 확인하고 녹음을 열거나 재생하지 않는다.

## 2단계: Apple Notes 첨부와 전사

메모의 「강의 녹음」 폴더 안에서 다음 과목 폴더를 사용한다: 미주지역지리→미주지역지리, 지도학및실습→지도학, 도시지리학특강→도시지리특강, 응용 지형학→응용지형학, 기후변화와 미래환경→기후변화. 각 녹음의 대상 폴더에서 제목이 M/D인 메모만 연다. 없으면 만들지 말고 보고한다.

- 같은 녹음인지 여부는 첨부 제목과 재생시간의 정규화된 값이 모두 같을 때만 인정한다. 제목 표시의 /와 : 차이는 정규화한다. 다른 오디오나 날짜·과목이 틀린 첨부가 있어도 올바른 녹음을 건너뛰지 말고 불일치를 보고한다.
- 기존 일치 첨부에 전사문 일부가 보이면 완료로 보고 추가하지 않는다. 새로 첨부한 뒤 해당 메모에서 제목·재생시간·전사문 표시만 확인한다. 재생 버튼은 누르지 않는다. 전사문이 안 보일 때만 첨부 카드의 본문/제목 영역을 더블클릭하고 최대 60초 기다린다.
- 실패 첨부는 삭제하지 않는다. 같은 메모에 새 복사본을 추가하고 확인한다. 이전 실행에서 남은 것까지 포함해 동일 원본의 일치 첨부가 총 5개가 되면 더 올리지 않는다. 하나라도 전사되면 즉시 완료, 5개 모두 실패하면 실패로 보고한다. Voice Memos 원본, 메모, 첨부를 절대 삭제하지 않는다.
- 후보 행을 한 번 수집하고 대상 메모별로 묶어 폴더·메모 매핑을 캐시한다. 매 파일마다 전체를 다시 훑지 말고, 마지막에는 이번 실행에서 건드린 메모와 실패 메모만 재확인한다.

## 보고

이름 변경, 새 첨부, 이미 완료되어 건너뜀, 폴더/메모 누락, 제목·시간 불일치, 전사 실패만 간단히 보고한다. 오디오 내용은 노출하거나 어디에도 업로드하지 않는다.
