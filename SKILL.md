---
name: lecture-recording-organizer
description: Rename Apple Voice Memos lecture recordings and attach each recording once to the matching Apple Note by date and course. Use for scheduled, manual, or catch-up runs; transcription handling is out of scope.
---

# 녹음-메모 자동화

학기 중 강의 녹음의 제목을 맞추고 정확한 과목·날짜 메모에 한 번만 첨부한다. 사용자에게 질문하거나 기다리지 않는다. 모호함·잠금·권한 문제는 건너뛰고 마지막에만 보고한다.

오디오를 재생하지 않는다. 녹음·메모·첨부를 삭제하지 않는다. Safari, Finder 미리보기, iPhone 미러링, Script Editor, 파일 선택 창을 사용하지 않는다. Voice Memos에서는 「모든 녹음 항목」만 사용하고 휴지통·최근 삭제 항목·다른 폴더는 열지 않는다. 전사 상태는 확인하거나 유도하지 않는다.

## 범위와 시간표

Asia/Seoul 기준 2026-09-01부터 2026-12-18까지 누락분을 매번 보충한다.

- 월: 09:00–10:30 미주지역지리, 13:00–15:00 지도학및실습, 16:30–18:00 도시지리학특강
- 화: 15:00–16:30 응용 지형학, 16:30–18:00 기후변화와 미래환경
- 수: 10:30–12:00 미주지역지리, 13:00–15:00 지도학및실습, 15:00–16:30 도시지리학특강
- 목: 15:00–16:30 응용 지형학, 16:30–18:00 기후변화와 미래환경
- 금: 수업 없음

판정은 Voice Memos DB의 `ZDATE` 시작 시각만 사용한다. 실제 수업 구간 `[시작, 종료)`을 우선하고, 다른 실제 수업 구간에 속하지 않을 때만 시작 15분 전까지 허용한다. 종료 후 허용시간은 없다. `ZDURATION <= 600초` 또는 `09:00 <= 시작 < 18:00`이 아닌 녹음은 계산·UI 확인·보고에서 완전히 제외한다.

## 빠른 실행

1. Voice Memos에서 「모든 녹음 항목」을 선택한 최신 AX 상태를 한 번 읽는다. 폴더가 실제로 선택됐는지 확인하고 각 행의 현재 제목·재생시간을 수집한다. 전체 개수가 표시되면 정확히 그만큼 모으고, 표시되지 않으면 목록 끝까지 스크롤해 새 행이 더 나오지 않을 때까지 고유 행을 모은다. 끝까지 확인하지 못하면 아무것도 변경하지 않고 `목록 미확인`으로 끝낸다.

2. 스킬 폴더에서 읽기 전용 플래너를 실행한다.

```bash
python3 scripts/plan.py --visible-json '<JSON>'
```

입력 형식은 `{"total_count": N, "rows": [{"title": "9월 17일 응용 지형학", "duration": "1시간, 17초"}]}`이다. 긴 입력은 `--visible-json -`와 표준입력을 사용해도 된다.

플래너가 다음을 한 번에 처리한다.

- Voice Memos DB와 실제 「모든 녹음 항목」 행의 제목·반올림 재생시간 교집합
- 기간·길이·시간대·시간표 판정과 분할 그룹 계산
- Notes의 정확한 과목 폴더·`M/D` 메모와 최상위 오디오 첨부 대조
- `.m4a`, `/`·`:`, Unicode 차이 정규화
- Notes의 파생 하위 첨부를 논리적 중복으로 잘못 세지 않기

`status=fatal`이면 UI를 변경하지 않는다. `actions`가 비어 있으면 Notes를 열지 않고 보고 후 종료한다. 파일·`ZPATH`·파일 선택 창에 보이는 `.m4a`는 후보 근거가 아니다.

## 변경 실행

플래너의 `actions`만 처리한다. 첫 변경 전에 후보와 분할 번호를 고정하고, 실행 중 새 항목을 끼워 넣지 않는다.

- `rename`: 최신 AX에서 제목·재생시간을 다시 확인한 뒤 Voice Memos 제목을 변경한다. 같은 `Z_PK`의 DB 제목을 즉시 재확인하고, 실패하면 한 번만 재시도한다.
- `attach`: 이름 변경 확인 후 Voice Memos에서 해당 행을 선택하고 `공유` → `메모`를 연다. 공유 시트의 기존 메모 선택 메뉴에서 `강의 녹음` → 플래너의 `note_folder` → `note_date`를 순서대로 선택한다. 공유 카드의 녹음 제목과 선택한 폴더·메모가 작업과 정확히 일치할 때만 `저장`한다. 다른 앱으로 드래그하거나 파일 선택 창으로 우회하지 않는다.
- 실제 변경 직전에만 `ZDURATION`과 원본 파일 크기를 3초 간격으로 한 번 비교한다. 변하면 녹음 중이므로 보류한다.

GUI는 단계 전환 뒤 최신 AX 상태를 읽고 이전 AX 인덱스를 재사용하지 않는다. 메뉴 안에서 이미 검증된 정확한 계층을 연속 선택하는 동작은 한 번에 묶을 수 있다. 선택된 행·창·목록·제목·재생시간이 예상과 다르면 그 실행의 모든 추가 GUI 변경을 즉시 중단한다. 공유 대상 메뉴 AX는 가능하면 출력하지 않고 정확한 `강의 녹음`·과목·날짜 경로만 추출하며, Notes의 전체 메모 목록을 보고 목적으로 펼쳐 읽지 않는다.

Notes에서 정확한 과목 폴더나 `M/D` 메모가 1개가 아니면 추측하거나 새로 만들지 않는다. 대상 메모에 유효 후보가 아닌 최상위 오디오 첨부나 동일 제목·재생시간 중복이 있으면 기존 첨부를 수정·삭제·재첨부하지 않고 `기존 첨부 이상`으로 보고한다.

`저장` 뒤에는 Notes UI를 다시 탐색하지 않고 읽기 전용 확인기를 실행한다.

```bash
python3 scripts/wait_for_attachment.py --note-pk <NOTE_PK> --title '<DESIRED_TITLE>' --duration-seconds <SECONDS>
```

확인기는 최대 30초 동안 최상위 오디오 첨부의 제목·재생시간을 확인한다. `complete`일 때만 다음 작업으로 진행하고, `duplicate`나 `timeout`이면 추가 변경을 멈추고 각각 `기존 첨부 이상` 또는 `첨부 불완전`으로 보고한다. 모든 변경 후 Voice Memos의 최신 전체 행으로 플래너를 한 번 더 실행한다. 처리한 항목에 같은 작업이 다시 나오면 실패로 보고한다.

## 이름과 보고

표준 이름은 `M월 D일 과목명` 또는 `M월 D일 과목명 i-n`이다. 예: `9월 15일 기후변화와 미래환경`, `9월 15일 기후변화와 미래환경 1-2`. 분할 번호는 이번 실행에서 확인된 유효 후보만으로 계산한다. 기존의 유효한 기본 제목이나 분할 번호는 재정렬하지 않는다.

이름 변경, 새 첨부, 이미 완료, 시간표 불일치, 이름 변경 실패, 메모 누락·중복, 첨부 불완전, 목록 미확인, 첨부 보류, 기존 첨부 이상, 잠금·권한 보류만 짧게 보고한다. 사전 제외한 짧은 녹음·시간 외 녹음은 보고하지 않는다.
