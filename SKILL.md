---
name: lecture-recording-organizer
description: Rename Apple Voice Memos lecture recordings and attach them to matching Apple Notes by date and course, including lightweight transcription checks and bounded retries. Use for this lecture-recording workflow only.
---

# 강의 녹음 정리 및 메모 첨부

Computer Use로 1단계와 2단계를 빠르게 처리한다. Asia/Seoul 시간을 사용한다. 이 시간표는 2026-09-01부터 2026-12-18까지만 유효하며, 그 밖의 날짜에는 아무것도 변경하지 않고 보고만 한다.

오디오를 재생하지 않는다. Voice Memos 원본과 Apple Notes 메모 자체는 삭제하지 않는다. 전사 재시도에 필요한 경우에만 실패가 확인된 해당 첨부 파일을 삭제할 수 있다.

## 범위와 시간표

- Voice Memos의 「모든 녹음 항목」 목록만 후보로 삼는다. 휴지통·최근 삭제 항목·다른 폴더·녹음 저장 디렉터리는 보거나 검색하지 않는다.
- Safari와 iPhone 미러링을 사용하지 않는다. Finder 미리보기, Space, 재생 버튼, Voice Memos 행 더블클릭도 하지 않는다. 녹음 중이거나 시간이 변하는 항목은 다음 실행으로 넘긴다.
- 한 번의 실행에서 목록 행을 한 번 수집한다. 1단계는 표준 이름이 아닌 항목(「새로운 녹음…」 포함)을 미처리 후보로 삼고, 표준 이름 항목은 즉시 건너뛴다. 2단계는 이번 실행에서 이름을 붙인 녹음과 오늘 날짜의 표준 이름 녹음만 대상으로 하며, 오래된 표준 이름 녹음은 매번 전수 재검사하지 않는다.
- 표준 이름은 `M/D 과목명` 또는 `M/D 과목명 1/n` 형식이다. 이 형식이 아닌 녹음은 기존 제목이 무엇이든 미처리 후보로 보고, 정확한 시작 시각과 시간표가 맞을 때만 이름을 바꾼다. 매칭 실패·시간표 밖·메타데이터 불명확 항목은 그대로 두고 보고한다.
- 메모 폴더·대상 날짜를 먼저 묶어 확인하고, 파일마다 전체 Voice Memos와 Notes를 다시 훑지 않는다. 마지막에는 이번 실행에서 건드린 메모와 보류·실패 항목만 재확인한다.
- 재생하지 않는 읽기 전용 메타데이터에서 정확한 시작 시각을 얻어 시간표 시작 시각과 가장 가까운 항목(±10분 이내)으로 매칭한다. 수업 길이는 매칭에 쓰지 않는다. 정확한 시작 시각을 얻지 못하면 추측하지 않고 건너뛰고 보고한다.
- 월 09:00 미주지역지리, 13:00 지도학및실습, 16:30 도시지리학특강
- 화 15:00 응용 지형학, 16:30 기후변화와 미래환경
- 수 10:30 미주지역지리, 13:00 지도학및실습, 15:00 도시지리학특강
- 목 15:00 응용 지형학, 16:30 기후변화와 미래환경
- 금요일 수업 없음

시간표 밖이거나 매칭이 유일하지 않으면 그대로 두고 짧게 보고한다. 같은 실행에서 같은 날짜·과목 녹음이 여러 개면 시작 시각 순으로 「M/D 과목명 1/n」, 「2/n」처럼 붙이고, 하나면 접미사를 붙이지 않는다. 나중에 새 녹음이 추가되어 기존 분할 번호를 다시 맞추는 작업은 하지 않는다.

## 1단계: Voice Memos 이름 변경

1. 「모든 녹음 항목」에서 후보 행의 제목과 파일 식별자를 한 번 수집한다.
2. 각 후보 행의 정확한 시작 시각을 읽기 전용 메타데이터로 확인한다.
3. 유일하게 매칭되는 미처리 후보만 이름을 바꾼다. 이미 표준 이름이 붙은 녹음은 다시 이름을 바꾸지 않는다.
4. 같은 목록에서 이름만 확인하고 녹음을 열거나 재생하지 않는다.

## 2단계: Apple Notes 첨부와 전사

「강의 녹음」 안의 다음 과목 폴더만 사용한다: 미주지역지리→미주지역지리, 지도학및실습→지도학, 도시지리학특강→도시지리특강, 응용 지형학→응용지형학, 기후변화와 미래환경→기후변화. 대상 폴더에서 제목이 정확히 M/D인 메모를 찾는다. 0개이거나 2개 이상이면 추측하지 말고 해당 녹음만 건너뛰고 보고한다. 메모 제목과 내부 날짜가 서로 다르면 동일하게 건너뛴다. 메모는 새로 만들지 않는다.

- 같은 녹음인지 여부는 첨부 제목과 재생시간의 정규화된 값이 모두 같을 때만 인정한다. 제목 표시의 /와 : 차이는 정규화한다. 다른 오디오나 날짜·과목이 틀린 첨부가 있어도 올바른 녹음은 별도로 처리하고 불일치를 보고한다.
- 기존 일치 첨부에 전사문 일부가 보이면 완료로 보고하고 추가하지 않는다. 새로 첨부한 뒤 해당 메모에서 제목·재생시간·전사문 표시만 확인한다. 재생 버튼은 누르지 않는다.
- 전사문이 안 보이면 첨부 카드의 본문·제목 영역만 더블클릭하고 최대 60초 기다린다. 전사문이 나타나면 즉시 완료한다.
- 60초 뒤에도 전사문이 없지만 앱에 명시적인 실패 표시가 없으면 `전사 대기`로 남기고 같은 녹음의 복사본을 추가하지 않는다. 다음 실행에서 해당 날짜의 보류 항목만 다시 확인한다.
- 앱에서 해당 첨부의 전사 실패가 명확히 확인될 때만 그 첨부 파일 하나를 삭제하고 같은 원본을 다시 첨부한다. 원본 Voice Memos 녹음이나 메모 자체는 삭제하지 않는다. 동일 원본은 총 5회까지만 시도하고, 하나라도 전사되면 즉시 완료한다. 5회 모두 실패하면 메모에 더 올리지 않고 실패로 보고한다.

## 보고

이름 변경, 새 첨부, 이미 완료되어 건너뜀, 보류·실패, 폴더/메모 누락, 제목·시간 불일치와 이번 실행의 검사 범위만 간단히 보고한다. 오디오 내용은 노출하거나 어디에도 업로드하지 않는다.
