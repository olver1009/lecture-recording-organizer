---
name: lecture-recording-organizer
description: Rename Apple Voice Memos lecture recordings and attach each recording once to the matching Apple Note by date and course. Use for scheduled, manual, or catch-up runs of this lecture workflow; transcription handling is out of scope.
---

# 녹음-메모 자동화

목표는 학기 중 강의 녹음의 제목을 맞추고 정확한 과목·날짜 메모에 파일 하나를 첨부하는 것이다. 전사 상태는 확인하거나 유도하지 않는다.

이 작업 중 사용자에게 질문하거나 응답을 기다리지 않는다. 판단이 모호하거나 화면 잠금·권한·앱 상태 때문에 처리할 수 없는 항목은 건너뛰고 마지막에만 보고한다. Voice Memos 제목 변경과 Notes 첨부 추가는 승인된 작업이다. 파일·녹음·메모·첨부를 삭제하지 않는다.

오디오를 재생하지 않는다. Safari·Finder 미리보기·iPhone 미러링·Script Editor를 사용하지 않는다. Voice Memos에서는 「모든 녹음 항목」만 다루며 휴지통·최근 삭제 항목·다른 폴더는 열지 않는다.

## 실행 범위

Asia/Seoul 기준 2026-09-01부터 2026-12-18까지의 녹음을 다룬다. 예약 실행과 수동 실행을 구분하지 않는다. 매번 Voice Memos DB에서 학기 시작일부터 오늘까지를 빠르게 대조해 다음 항목만 처리한다.

- 시간표상 강의인데 제목이 맞지 않는 녹음
- 제목은 맞지만 Notes에 같은 제목·재생시간의 첨부가 없는 녹음

이미 제목과 첨부가 모두 맞는 녹음은 UI에서 다시 열거나 검토하지 않는다. 따라서 예약을 놓친 날짜가 있어도 다음 실행에서 누락분을 보충한다.

GUI 작업을 시작할 때 로그인 세션이 열려 있으면 `caffeinate -diu -t 900`으로 이번 실행 중 유휴 잠금을 예방한다. 이미 화면이 잠겨 있거나 앱 제어 권한을 새로 요구하면 기다리거나 질문하지 말고 처리 가능한 읽기 작업만 끝낸 뒤 `화면 잠금/권한으로 보류`라고 보고한다. 다음 실행에서 다시 대조한다.

## 고정 시간표

- 월 09:00–10:30 미주지역지리, 13:00–15:00 지도학및실습, 16:30–18:00 도시지리학특강
- 화 15:00–16:30 응용 지형학, 16:30–18:00 기후변화와 미래환경
- 수 10:30–12:00 미주지역지리, 13:00–15:00 지도학및실습, 15:00–16:30 도시지리학특강
- 목 15:00–16:30 응용 지형학, 16:30–18:00 기후변화와 미래환경
- 금요일 수업 없음

Voice Memos DB의 `ZDATE` 시작 시각만 사용한다. 각 수업의 실제 구간은 반열린 구간 `[시작, 종료)`로 정의한다. 녹음 시작 시각을 `t`라고 할 때 기본 후보 조건은 `시작 - 15분 <= t < 종료`이다. 따라서 ±15분은 수업 시작 전 조기 시작 허용시간일 뿐, 수업 시작 시각과 가까워야 한다는 조건이 아니다. 수업 시작 후 15분이 지나도 종료 전 시작한 녹음이면 정상 후보로 포함한다. 파일 생성·수정·종료 시각과 재생시간은 과목 판정에 쓰지 않는다.

인접 수업의 실제 구간 경계는 겹치지 않는다. 조기 시작 허용범위가 다른 수업의 실제 구간과 겹치는 경우에는 실제 구간에 포함되는 수업을 우선하고, 어느 실제 구간에도 속하지 않을 때만 조기 시작 허용범위를 적용한다. 그래도 유일하게 정할 수 없으면 추측하지 않고 해당 녹음을 건너뛴다. `시간표 불일치`는 녹음 시작 시각이 그 날짜의 어떤 수업 후보 구간에도 속하지 않을 때만 보고한다.

예를 들어 2026-09-15 16:30–18:00 기후변화와 미래환경 수업의 16:31:31 및 17:25:44 녹음은 모두 정상 후보이며, 21:13:45 녹음과 금요일 녹음은 시간표 불일치다. 수업 시작 10–15분 전은 허용하고, 별도 종료 후 허용시간은 두지 않는다.

시간표 판정 전에 `ZDURATION <= 600초`인 레코드는 즉시 제외한다. 이런 녹음은 이름·시간표·Notes 매칭을 계산하지 않고, UI를 확인하지 않으며, `시간표 불일치`로도 보고하지 않는다. 그 다음 현지 시작 시각이 `09:00 <= t < 18:00`인 레코드만 남긴다. 이 범위 밖의 녹음도 시간표 계산·UI 확인·불일치 보고 없이 제외한다.

표준 이름은 `M/D 과목명` 또는 `M/D 과목명 1/n`이다. 현재 제목이 무엇이든 시작 시각으로 계산한 이름과 다르면 수정 후보로 삼는다. 같은 날짜·과목으로 판정된 녹음이 여러 개면 시작 시각 순으로 `1/n`, `2/n`을 붙일 수 있다. 이전에 사용자가 붙인 분할 번호는 다시 정렬하지 않는다.

분할 번호를 계산하는 집합은 DB에 존재하는 모든 파일이 아니라, 이번 실행에서 `모든 녹음 항목`에 실제로 보이고 고유하게 확인된 후보만으로 고정한다. 목록에 없는 녹음, 파일 시스템이나 파일 선택 창에서만 보이는 `.m4a`, 또는 동일 제목·재생시간 때문에 어느 행인지 구분할 수 없는 녹음은 분할 수에 포함하지 않는다. 같은 날짜·과목에 확인된 후보가 하나뿐이면 `M/D 과목명`을 사용하고, 보이지 않는 녹음을 근거로 `2/2` 같은 접미사를 만들지 않는다. 이미 사용자가 붙인 번호는 자동으로 재번호 매기지 않는다.

## 빠른 대조와 이름 변경

다음 DB를 읽기 전용으로 한 번 조회한다.

`~/Library/Group Containers/group.com.apple.VoiceMemos.shared/Recordings/CloudRecordings.db`

```sql
SELECT Z_PK, ZUNIQUEID, ZCUSTOMLABELFORSORTING, ZPATH,
       datetime(ZDATE + 978307200, 'unixepoch', 'localtime') AS started_local,
       ZDURATION
FROM ZCLOUDRECORDING
WHERE ZPATH IS NOT NULL AND ZDURATION > 600
ORDER BY ZDATE;
```

코드에서 실행 기간과 오늘 이전 레코드만 남기고 시간표를 대조한다. 실제로 변경하거나 첨부할 후보만 Voice Memos 「모든 녹음 항목」에 존재하는지 확인한다. 목록에 없으면 휴지통 여부를 추측하지 말고 건너뛴다.

이 확인은 파일명, `ZPATH`, 원본 파일의 존재, 또는 파일 첨부 창의 표시만으로 통과시킬 수 없다. `모든 녹음 항목`에서 현재 제목과 재생시간으로 DB 행을 고유하게 대응할 수 있어야 한다. 대응이 안 되거나 목록 상태와 DB가 다르면 해당 후보를 `목록 미확인`으로 건너뛰고 Notes를 열거나 파일 선택 창을 열지 않는다. 후보 목록과 분할 번호는 첫 GUI 변경 전에 확정하며, 이후 새로 나타난 파일을 그 실행에 끼워 넣지 않는다.

이름 변경 직전 `ZDURATION`과 원본 파일 크기를 3초 간격으로 한 번 비교한다. 변하면 녹음 중이므로 보류한다. Computer Use로 제목을 바꾼 뒤 같은 `Z_PK`의 `ZCUSTOMLABELFORSORTING`만 다시 읽는다. 다르면 한 번만 재시도하고, 그래도 다르면 Notes에 첨부하지 않고 `이름 변경 실패`로 보고한다.

각 GUI 동작 뒤에는 최신 AX 상태를 다시 읽고 대상과 선택 상태를 재확인한다. 이전 AX 인덱스를 재사용하지 않는다. 예상과 다른 행이 선택되었거나 창·목록·제목·재생시간 중 하나라도 달라지면 즉시 그 실행의 모든 추가 GUI 변경을 중단하고 보고한다. 다른 파일을 시험 선택하거나 대체 경로로 복구하지 않는다.

## Notes 첨부

과목→Notes 폴더는 다음과 같다: 미주지역지리→미주지역지리, 지도학및실습→지도학, 도시지리학특강→도시지리특강, 응용 지형학→응용지형학, 기후변화와 미래환경→기후변화.

Notes DB를 읽기 전용으로 조회해 정규화한 첨부 제목과 초 단위 반올림 재생시간이 원본과 모두 같은지 먼저 확인한다. 일치하면 완료로 보고 UI를 열지 않는다. 제목은 `/`와 `:` 및 `.m4a` 확장자 차이를 정규화한다.

일치 첨부가 없을 때만 Notes의 「강의 녹음」 아래 정확한 과목 폴더와 제목이 정확히 `M/D`인 메모를 연다. 메모가 0개 또는 2개 이상이면 추측하거나 질문하지 않고 건너뛴다. 메모를 새로 만들지 않는다.

Script Editor와 AppleScript GUI는 사용하지 않는다. 이름 변경이 확인된 Voice Memos 항목을 대상 메모로 직접 드래그하는 방식만 자동 실행에서 사용한다. 자동 드래그가 실패하거나 AX 상태로 원본 행과 대상 메모를 고유하게 확인할 수 없으면 `첨부 보류`로 보고하고 파일 첨부 UI를 열지 않는다. 원본 `.m4a`가 파일 시스템이나 파일 선택 창에 남아 있다는 사실은 `모든 녹음 항목` 소속의 증거가 아니다. 첨부 제목이 표준 이름과 다르면 해당 첨부 하나만 이름을 바꾼다.

이미 대상 메모에 이번 실행의 유효 후보가 아닌 녹음이 첨부되어 있거나, 같은 제목·재생시간의 논리적 첨부가 둘 이상이면 기존 첨부를 삭제·수정·재첨부하지 말고 그 메모의 추가 변경을 중단한다. 이를 자동으로 정상 상태로 간주하지 않는다.

첨부 후 해당 메모에서 제목과 재생시간만 원본과 대조한다. 복사 중이면 최대 30초까지만 기다린다. 30초 뒤에도 재생시간이 0이거나 원본과 다르면 삭제·재첨부·질문 없이 `첨부 불완전`으로 보고한다. 전사문, 전사 버튼, 요약 패널은 열거나 확인하지 않는다.

## 보고

이름 변경, 새 첨부, 이미 완료, 시간표 불일치, 이름 변경 실패, 메모 누락·중복, 첨부 불완전, 목록 미확인, 첨부 보류, 기존 첨부 이상, 화면 잠금·권한 보류만 짧게 보고한다. 사전 제외한 짧은 녹음·시간 외 녹음은 보고하지 않는다. 오디오 내용과 전사문은 보고하지 않는다.
